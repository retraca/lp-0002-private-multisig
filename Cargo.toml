[workspace]
members = [
    "circuit/guest",
    "circuit/host",
    "sdk",
]
# programs/multisig depends on nssa-core (private LEZ dep) -- not compilable standalone
exclude = ["programs/multisig"]
resolver = "2"
