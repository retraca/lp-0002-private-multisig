[workspace]
members = [
    "circuit/guest",
    "circuit/host",
    "sdk",
]
# programs/* depend on nssa-core (private LEZ dep) -- not compilable standalone
exclude = ["programs/multisig", "programs/vote_circuit"]
resolver = "2"
