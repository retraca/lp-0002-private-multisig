[package]
name = "private-multisig-guest"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "private-multisig-guest"
path = "src/main.rs"
test = false

[dependencies]
risc0-zkvm = { version = "3.0.5", default-features = false, features = ["std"] }
serde = { version = "1", default-features = false, features = ["derive", "alloc"] }
