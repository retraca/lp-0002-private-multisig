[package]
name = "private-multisig-sdk"
version = "0.2.0"
edition = "2021"

[dependencies]
anyhow = "1"
borsh = { version = "1", features = ["derive"] }
hex = "0.4"
private-multisig-program = { path = "../programs/multisig" }
